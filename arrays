#find duplicate number

class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        t=h=nums[0]
        t=nums[t]
        h=nums[nums[h]]
        while t!=h:
            t=nums[t]
            h=nums[nums[h]]
        t=nums[0]
        while t!=h:
            t=nums[t]
            h=nums[h]
        return t
